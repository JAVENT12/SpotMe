@model IEnumerable<Identity.Models.Routine>

    <table class="table table-striped table-bordered table-sm" id="myTable">

        <thead>
            <tr>
                <th class="text-center">
                   Routines
                </th>


            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>

                    <td class="text-center">
                        @*<a asp-action="Create" style="color:black">Create WorkOuts!, </a>*@
                        @*@Html.DisplayFor(modelItem => item.name)
                        |*@
                        <a asp-action="Description" style="color:white" asp-route-id="@item.muscleGroupID"> @Html.DisplayFor(modelItem => item.UserName)</a>
                        @if (User?.Identity?.IsAuthenticated ?? true)
                        {
                            @*<a asp-action="Description" style="color:white" asp-route-id="@item.muscleGroupID"> @Html.DisplayFor(modelItem => item.name)</a>*@
                        }
                        else
                        {
                            @*<a asp-action="Description" style="color:white" asp-route-id="@item.muscleGroupID"> @Html.DisplayFor(modelItem => item.name)</a>*@

                        }
                    </td>

                </tr>
            }
        </tbody>
    </table>