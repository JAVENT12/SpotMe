@*@model IEnumerable<Identity.Models.MuscleGroup>*@
@model Identity.Models.ViewModel.MuscleGroupListViewModel

@{
    ViewData["Title"] = "Index";
}

<head>
    <link rel="stylesheet" href="~/css/ButtonSheet.css" />

</head>
@*<input type="text" id="myInput" onkeyup="myFunction()" placeholder="Search for names..">
<script>
    function myFunction() {
        // Declare variables
        var input, filter, table, tr, td, i, txtValue;
        input = document.getElementById("myInput");
        filter = input.value.toUpperCase();
        table = document.getElementById("myTable");
        tr = table.getElementsByTagName("tr");

        // Loop through all table rows, and hide those who don't match the search query
        for (i = 0; i < tr.length; i++) {
            td = tr[i].getElementsByTagName("td")[0];
            if (td) {
                txtValue = td.textContent || td.innerText;
                if (txtValue.toUpperCase().indexOf(filter) > -1) {
                    tr[i].style.display = "";
                } else {
                    tr[i].style.display = "none";
                }
            }
        }
    }
</script>*@
<table class="table table-striped table-bordered table-sm" id="myTable">

    <thead>
        <tr>
            <th class="text-center">
                Exercises
            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.MuscleGroups)
        {
            <tr>

                <td class="text-center">
                    @*<a asp-action="Create" style="color:black">Create WorkOuts!, </a>*@
                    @*@Html.DisplayFor(modelItem => item.name)
        |*@
                    <a asp-action="Description" style="color:white" asp-route-id="@item.muscleGroupID"> @Html.DisplayFor(modelItem => item.name)</a>
                    @if (User?.Identity?.IsAuthenticated ?? true)
                    {
                        @*<a asp-action="Description" style="color:white" asp-route-id="@item.muscleGroupID"> @Html.DisplayFor(modelItem => item.name)</a>*@
                    }
                    else
                    {
                        @*<a asp-action="Description" style="color:white" asp-route-id="@item.muscleGroupID"> @Html.DisplayFor(modelItem => item.name)</a>*@

                    }
                </td>

            </tr>
        }
    </tbody>
</table>

<div page-model="@Model.PagingInfo" page-action="Excercises" page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-secondary"
     page-class-selected="btn btn-success pull-right" page-url-category="@Model.CurrentCategory"
     class="btn-group pull-right m-1">
</div>