@model Identity.Models.MuscleGroup

@{
    ViewData["Title"] = "Description";

}




<div>
    <link rel="stylesheet" href="~/css/main.css" />

    <table class="table table-striped table-bordered table-sm">

        <thead>
            <tr>


                <th class="text-center" >
                    @Html.DisplayFor(model => model.name)

                </th>


            </tr>
        </thead>
        <tbody>

            <tr>



                <td  style="color:white">
                    @Html.DisplayFor(model => model.description)
                </td>

                

            </tr>
            <tr>
                <td>
                    <a asp-action="Excercises" style="color:gray">Back to List</a>
                    @if (User?.Identity?.IsAuthenticated ?? true)
                    {
                        <a asp-action="Description" style="color:white" asp-route-id="@Model.muscleGroupID"> @*@Html.DisplayFor(modelItem => Model.name)*@</a>
                        <form id="@Model.muscleGroupID" asp-action="AddToRoutine"
                              asp-controller="Routine" method="post">
                            <input type="hidden" asp-for="@Model.muscleGroupID" />
                            <input type="hidden" name="returnUrl"
                                   value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                            <span class="card-text p-1">
                                @*@item.description*@
                                <button type="submit"
                                        class="btn btn-success btn-sm pull-right" style="float:right">
                                    Add to Routine
                                </button>
                            </span>
                        </form>
                    }
                    else
                    {
                        @*<a asp-action="Description" style="color:white" asp-route-id="@item.muscleGroupID"> @Html.DisplayFor(modelItem => item.name)</a>*@
                        <a asp-action="Description" style="color:white" asp-route-id="@Model.muscleGroupID"> @*@Html.DisplayFor(modelItem => Model.name)*@</a>
                        <form id="@Model.muscleGroupID" asp-action="AddToRoutine"
                              asp-controller="Routine" method="post">
                            <input type="hidden" asp-for="@Model.muscleGroupID" />
                            <input type="hidden" name="returnUrl"
                                   value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                            <span class="card-text p-1">
                                @*@item.description*@
                                <button type="submit"
                                        class="btn btn-success btn-sm pull-right" style="float:right">
                                    Add to Routine
                                </button>
                            </span>
                        </form>
                    }

                </td>
            </tr>

        </tbody>

    </table>






    @*<dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.WorkoutsID)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.WorkoutsID)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.name)
            </dd>
        </dl>*@
</div>
<div>
</div>
